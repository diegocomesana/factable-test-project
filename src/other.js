// FACTABLE

// ESTE CASO NO FUNCIONA BIEN PORQUE NO TOMA EL NOMBRE DEL LA FUNCIÓN
const salida = {
  primeraFunc: (param) => {
    return "algo primeraFunc" + param;
  },
  segundaFunc: (param) => {
    return "algo segundaFunc" + param;
  },
};

module.exports = {
  salida,
};
